name: "install uv"
description: "installs uv using the community action"
runs:
  using: composite
  steps:
    - name: install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "0.7.8"

    - name: sync dev dependencies
      uses: uv sync --extra dev